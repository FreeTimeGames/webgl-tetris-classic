stages:
  - deploy

pages:
  stage: deploy
  script:
    - mv ./deploy ./public  # Di chuyển thư mục build WebGL vào thư mục public
    - echo "Deploying pages"
  artifacts:
    paths:
      - public
  only:
    - dev